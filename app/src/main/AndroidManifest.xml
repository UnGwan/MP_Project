<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <uses-feature android:name="android.hardware.camera"/>
    <uses-feature android:name="android.hardware.camera.autofocus"/>

    <application
        android:allowBackup="true"
        android:appComponentFactory="android.support.v4.app.CoreComponentFactory"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Day_28App"
        android:usesCleartextTraffic="true"
        tools:replace="android:appComponentFactory"
        tools:targetApi="31">
        <activity
            android:name=".Main1_Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <!-- 로그인 회원가입 선택화면 -->
        <activity android:name=".login_Signup_Activity" />
        <!-- 회원가입 화면 -->
        <activity android:name=".Signup_Activity" />
        <!-- 로그인 화면-->
        <activity android:name=".Login_Activity" />
        <!-- 처음 소개 화면-->
        <activity android:name=".Intro_Activity" />
        <!-- 회원가입 성공시 화면-->
        <activity android:name=".Success_Signup_Activity" />
        <!-- 비밀번호 재설정 화면-->
        <activity android:name=".Password_Find_Activity" />
        <!-- 닉네임 설정 화면-->
        <activity android:name=".Set_Username_Activity"/>
        <!-- 카메라 액티비티-->
        <activity android:name=".CameraActivity" />

        <!-- 1주차 미션화면 -->
        <activity android:name=".Main_mission_1weeks_Activity"/>
        <!-- 미션 인증 화면 -->
        <activity android:name=".Complete_Mission_Page"/>
    </application>

</manifest>